import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CadastroProduto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
